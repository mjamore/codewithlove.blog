---
author: Michael Amore
authorImage: /images/michael-amore.jpeg
date: 12/20/2022
description: It can be surprisingly tricky to center a div in CSS. This article explains a few of the common techniques used to center a div (or any element) both horizontally and vertically in CSS.
image: /images/blog/css/thumb2_how-to-center-a-div.svg
tags: [CSS, Flexbox, Grid]
title: How to Center a div in CSS
url: /topics/css/how-to-center-a-div
---

## TL;DR

The optimal way to center an element in CSS will vary depending on which of the following circumstances apply to your situation:

1. Do you want to center the element horizontally or vertically (or both)?
2. Are you trying to center a block element or an inline element?
3. Do you know the width/height of the element or not.

The most universally applicable solution is Flexbox, which is detailed in option 2 below.

### Option 1: `margin: auto`

One of the most common ways to center a div is by using the `margin: auto` property. This method works by setting the left and right margins to auto, which evenly distributes the remaining space around the div. To center a div using this method, you can use the following code:

```css
div {
  width: 200px;
  margin: 0 auto;
}
```

##### Pros:

- Simple and easy to use
- Works with both block and inline elements

##### Cons:

- Only works if the width of the element is set
- Doesn't work if the element is floated

### Option 2: `display: flex`

Another way to center a div is by using the `display: flex` property. This method works by setting the parent element to display: flex and aligning the child element to the center using the `align-items: center` property. To center a div using this method, you can use the following code:

```css
.parent {
  display: flex;
  align-items: center;
  justify-content: center;
}

.child {
  width: 200px;
}
```

##### Pros:

- Flexible and can be used to center elements vertically and horizontally
- Works with both block and inline elements

##### Cons:

- Requires the use of additional properties (align-items and justify-content)
- Not supported in older browsers

### Option 3: `text-align: center`

If you are trying to center an inline element, such as a button or a link, you can use the `text-align: center` property. This method works by aligning the text within the element to the center. To center an inline element using this method, you can use the following code:

```css
button {
  display: inline-block;
  text-align: center;
}
```

##### Pros:

- Simple and easy to use
- Works with inline elements

##### Cons:

- Only works with inline elements
- Does not work with block elements

### Option 4: `position: absolute` and `transform: translate`

If you want to center an element using a more advanced method, you can use the `position: absolute` and `transform: translate` properties. This method works by setting the position of the element to absolute and using the translate function to center the element. To center a div using this method, you can use the following code:

```css
div {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}
```

##### Pros:

- Flexible and can be used to center elements vertically and horizontally
- Works with both block and inline elements

##### Cons:

- Requires the use of additional properties (top, left, and transform)
- May cause issues with other elements on the page

### Wrap It Up

In conclusion, there are several different ways to center a div in CSS. Each method has its own pros and cons, and it's important to choose the one that best fits your needs. If you are looking for a simple solution, you can use the `margin: auto` property. If you are looking for a more advanced solution, you can use the `position: absolute` and `transform: translate` properties. If you are looking for a solution that works with inline elements, you can use the `text-align: center` property. If you are looking for a solution that works with both block and inline elements, you can use the `display: flex` property.

### Sources

- <ExternalLink href='https://css-tricks.com/centering-css-complete-guide/'>CSS-tricks.com</ExternalLink>
- <ExternalLink href='https://openai.com/blog/chatgpt/'>ChatGPT</ExternalLink>
